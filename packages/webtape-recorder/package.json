{
  "name": "webtape-recorder",
  "version": "1.0.0",
  "description": "WebTape Chrome Extension — records web actions and network requests for LLM analysis.",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "node scripts/build.mjs",
    "build": "node scripts/build.mjs && mkdir -p dist && rm -f dist/webtape.zip && zip -r dist/webtape.zip manifest.json background.js content.js popup.html popup.js popup.css record-launcher.html record-launcher.js icons/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FurtherBank/WebTape.git",
    "directory": "packages/webtape-recorder"
  },
  "homepage": "https://github.com/FurtherBank/WebTape/tree/main/packages/webtape-recorder",
  "keywords": [
    "chrome-extension",
    "recording",
    "automation",
    "llm"
  ],
  "license": "Apache-2.0",
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
